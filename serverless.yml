service: stopinstances

provider:
  name: aws
  runtime: nodejs12.x

functions:
  rds:
    handler: functions/rdsStop.handler
    events:
      - schedule: cron(0 12 * * ? *)
  ec2:
    handler: functions/ec2Stop.handler
    events:
      - schedule: cron(0 12 * * ? *)